{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Run Extension (with demo)",
      "type": "extensionHost",
      "request": "launch",
      "args": [
        "--extensionDevelopmentPath=${workspaceFolder}",
        "${workspaceFolder}/demo"
      ]
    }
  ]
}
